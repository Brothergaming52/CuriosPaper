# Configuration for CuriosPaper Custom Accessory Slots

# DO NOT EDIT THIS
version: "1.3.2"

# Update Checker
updates:
  check-for-updates: true

# Resource Pack Settings
resource-pack:
  # Resource pack hosting mode:
  # - SELF: Automatically generate and host the resource pack on this server using an embedded HTTP server.
  # - LINK: Deliver the pack via a direct external download link (configured in 'url' below).
  # - NONE: Disable automatic resource pack downloading/hosting completely.
  mode: SELF

  # Direct download link for the resource pack (only used if mode is LINK)
  # IMPORTANT: This must be a direct download link (the URL must start downloading the file immediately, not open a web page).
  url: "https://example.com/your-resource-pack.zip"

  # (Legacy option) Enable automatic resource pack generation and serving (fallback if mode is not specified)
  enabled: true
  # Port for the embedded HTTP server (only used if mode is SELF)
  # You need a seperate port using the default port will not work and may break things if tried
  port: 8080
  # Public IP or Hostname of the server (for players to download the pack - only used if mode is SELF)
  host-ip: "localhost"
  # Base material for custom icons (usually PAPER or LEATHER_HORSE_ARMOR)
  base-material: "PAPER"

  # Whether to look in the "external-resource-packs" folder and combine zips into the main pack
  combine-external-rp: false

  allow-minecraft-namespace: true

  allow-namespace-conflicts: false

# Slot configurations - 9 total slot types
slots:
  # Head slot for helmets, crowns, circlets
  head:
    name: "Head"
    icon: "GOLDEN_HELMET"
    item-model: "curiospaper:head_slot"
    custom-model-data: 10001
    amount: 1
    lore:
      - "&7Equip crowns, circlets, or magical headpieces."

  # Necklace slot for amulets and pendants
  necklace:
    name: "Necklace"
    icon: "NAUTILUS_SHELL"
    item-model: "curiospaper:necklace_slot"
    custom-model-data: 10002
    amount: 1
    lore:
      - "&7Wear powerful amulets and pendants."

  # Back slot for capes and cloaks
  back:
    name: "Back"
    icon: "ELYTRA"
    item-model: "curiospaper:back_slot"
    custom-model-data: 10003
    amount: 1
    lore:
      - "&7Equip magical capes and cloaks."

  # Body slot for chest accessories
  body:
    name: "Body"
    icon: "DIAMOND_CHESTPLATE"
    item-model: "curiospaper:body_slot"
    custom-model-data: 10004
    amount: 1
    lore:
      - "&7Wear chest talismans and armor attachments."

  # Belt slot for utility items
  belt:
    name: "Belt"
    icon: "LEATHER"
    item-model: "curiospaper:belt_slot"
    custom-model-data: 10005
    amount: 1
    lore:
      - "&7Equip utility belts and sashes."

  # Hands/Gloves slots
  hands:
    name: "Hands"
    icon: "LEATHER_CHESTPLATE"
    item-model: "curiospaper:hands_slot"
    custom-model-data: 10006
    amount: 2
    lore:
      - "&7Magical gloves for both hands."

  # Bracelet slots for wrists
  bracelet:
    name: "Bracelet"
    icon: "IRON_CHAIN"
    item-model: "curiospaper:bracelet_slot"
    custom-model-data: 10007
    amount: 2
    lore:
      - "&7Wear enchanted bracelets and bangles."

  # Ring slots for fingers
  ring:
    name: "Ring"
    icon: "GOLD_NUGGET"
    item-model: "curiospaper:ring_slot"
    custom-model-data: 10008
    amount: 2
    lore:
      - "&7Holds powerful magical rings."

  # Charm slots for misc trinkets
  charm:
    name: "Charm"
    icon: "EMERALD"
    item-model: "curiospaper:charm_slot"
    custom-model-data: 10009
    amount: 4
    lore:
      - "&7Holds small, potent charms and trinkets."

# Feature toggles
features:
  # Enable the in-game item editor and custom item system
  # If disabled, custom items, recipes, loot tables,(made using this plugin) and the /edit command will be disabled
  item-editor:
    enabled: true

  # Allow elytra equipping in back slots
  allow-elytra-on-back-slot: true

  # Allow players to see empty accessory slots in GUI
  show-empty-slots: true

  # Use vanilla items for accessory slot icons (no custom models)
  use-vanilla-slot-icons: false

  # Play sound when opening GUI
  play-gui-sound: true
  gui-sound: "BLOCK_CHEST_OPEN"

  # Play sound when equipping item
  play-equip-sound: true
  equip-sound: "ENTITY_ITEM_PICKUP"

  # Play sound when unequipping item
  play-unequip-sound: true
  unequip-sound: "ENTITY_ITEM_PICKUP"

  # Hotkey system to open accessory GUI
  hotkey:
    enabled: true
    slot: 8  # Which hotbar slot (1-9) must be selected
    sneak-type: "double"  # Options: single, double, hold
    sneak-hold-duration: 3  # Seconds to hold sneak (for 'hold' type)

  keep-curio-inventory:
    # it can be "Always", "Auto" and "Never"
    # - Always makes the player keep their curios inventory even when keep inventory is false
    # - Auto drops or keeps the inventory according to the gamerule keepinventory (also applies for regions where drops are enabled and disabled)
    # - Never makes the player drop their inventory even when keep inventory is true
    type: "Auto"

  # Random Teleport (RTP) trigger settings
  # You can record these interactively in-game using /curios recordrtp
  rtp:
    # Set to false to completely disable all RTP compatibility checks and recordings
    enabled: false
    # Commands that trigger temporary model dismounting
    # You can use the in game recordrtp command to record these interactively
    commands:
      - "rtp"
      - "wild"
    # Interacted blocks (materials or "world,x,y,z") that trigger temporary model dismounting
    blocks: []
    # Entities/NPC names that trigger temporary model dismounting
    entities: []
    # GUIs (titles or "title:slot") that trigger temporary model dismounting
    guis: []

# Storage settings
storage:
  # Storage type (currently only 'yaml' is supported)
  type: "yaml"

  # Auto-save interval in seconds (300 = 5 minutes)
  # Set to 0 to disable auto-save
  save-interval: 300

# Debug settings
debug:
  # Enable debug logging
  enabled: false

  # Log inventory interactions
  log-inventory-events: false